// editor1
const watchdog = new CKSource.EditorWatchdog();

window.watchdog = watchdog;

watchdog.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog.setDestructor(editor => {


    return editor.destroy();
});

watchdog.on('error', handleError);

watchdog
    .create(document.querySelector('#editor1'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}
// editor2
const watchdog2 = new CKSource.EditorWatchdog();

window.watchdog2 = watchdog2;

watchdog2.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog2.setDestructor(editor => {


    return editor.destroy();
});

watchdog2.on('error', handleError);

watchdog2
    .create(document.querySelector('#editor2'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor3
const watchdog3 = new CKSource.EditorWatchdog();

window.watchdog3 = watchdog3;

watchdog3.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog3.setDestructor(editor => {


    return editor.destroy();
});

watchdog3.on('error', handleError);

watchdog3
    .create(document.querySelector('#editor3'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor4
const watchdog4 = new CKSource.EditorWatchdog();

window.watchdog4 = watchdog4;

watchdog4.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog4.setDestructor(editor => {


    return editor.destroy();
});

watchdog4.on('error', handleError);

watchdog4
    .create(document.querySelector('#editor4'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}



// editor5
const watchdog5 = new CKSource.EditorWatchdog();

window.watchdog5 = watchdog5;

watchdog5.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog5.setDestructor(editor => {


    return editor.destroy();
});

watchdog5.on('error', handleError);

watchdog5
    .create(document.querySelector('#editor5'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor6
const watchdog6 = new CKSource.EditorWatchdog();

window.watchdog6 = watchdog6;

watchdog6.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog6.setDestructor(editor => {


    return editor.destroy();
});

watchdog6.on('error', handleError);

watchdog6
    .create(document.querySelector('#editor6'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor7
const watchdog7 = new CKSource.EditorWatchdog();

window.watchdog7 = watchdog7;

watchdog7.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog7.setDestructor(editor => {


    return editor.destroy();
});

watchdog7.on('error', handleError);

watchdog7
    .create(document.querySelector('#editor7'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor8
const watchdog8 = new CKSource.EditorWatchdog();

window.watchdog8 = watchdog8;

watchdog8.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog8.setDestructor(editor => {


    return editor.destroy();
});

watchdog8.on('error', handleError);

watchdog8
    .create(document.querySelector('#editor8'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor9
const watchdog9 = new CKSource.EditorWatchdog();

window.watchdog9 = watchdog9;

watchdog9.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog9.setDestructor(editor => {


    return editor.destroy();
});

watchdog9.on('error', handleError);

watchdog9
    .create(document.querySelector('#editor9'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor10
const watchdog10 = new CKSource.EditorWatchdog();

window.watchdog10 = watchdog10;

watchdog10.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog10.setDestructor(editor => {


    return editor.destroy();
});

watchdog10.on('error', handleError);

watchdog10
    .create(document.querySelector('#editor10'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}


// editor11
const watchdog11 = new CKSource.EditorWatchdog();

window.watchdog11 = watchdog11;

watchdog11.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog11.setDestructor(editor => {


    return editor.destroy();
});

watchdog11.on('error', handleError);

watchdog11
    .create(document.querySelector('#editor11'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}

// editor12
const watchdog12 = new CKSource.EditorWatchdog();

window.watchdog12 = watchdog12;

watchdog12.setCreator((element, config) => {
    return CKSource.Editor
        .create(element, config)
        .then(editor => {


            return editor;
        })
});

watchdog12.setDestructor(editor => {


    return editor.destroy();
});

watchdog12.on('error', handleError);

watchdog12
    .create(document.querySelector('#editor12'), {

        licenseKey: '',
        removePlugins: ['Title']

    })
    .catch(handleError);

function handleError(error) {
    console.error('Oops, something went wrong!');
    console.error('Please, report the following error on https://github.com/ckeditor/ckeditor5/issues with the build id and the error stack trace:');
    console.warn('Build id: veri7bacd517-mgta3ymzjm3y');
    console.error(error);
}





